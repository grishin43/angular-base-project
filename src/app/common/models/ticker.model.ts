export interface TickerModel {
  symbol: string;
  directPrice: number;
  customDirectPrice: number;
  oppositePrice: number;
  customOppositePrice: number;
  quoteVolume: number;
  priceChangePercent: number;
}
